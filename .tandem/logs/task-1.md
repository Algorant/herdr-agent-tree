---
id: task-1
type: task
title: "Add a real test suite for agent-tree"
effort: "medium"
relatedFiles: ["src/identity.rs", "src/forest.rs", "src/decoration.rs", "src/pause.rs", "README.md", "docs/agent-tree/m2-contract.md"]
tags: ["release-readiness", "testing"]
accord:
  status: "accepted"
  acceptance: ["The identity rules are covered: hash recomputation, agency_self self-validation, unique parent resolution, and the tokenless-parent refinement.", "Every degenerate case in the contract C5 table has a test asserting the agent is left unlinked.", "Preorder emission, family contiguity, deterministic sibling order and unranked native ordering are covered.", "Rank format and decoration grammar are covered, including the 20-character cap and its drop order.", "Paused behaviour is covered: paused publishes nothing, apply clears the flag, clear does not.", "The suite runs with a single documented command and needs no running Herdr server.", "The suite passes and the command is recorded in README.md."]
  claimedAt: "2026-09-16T02:07:00Z"
  deliveredAt: "2026-09-16T02:14:53Z"
  validation: ["$ git diff --check", "$ cargo test --locked"]
  constraints: ["No Herdr server harness and no dependency on a live or isolated server; demo.sh remains the end-to-end path.", "Do not change plugin behaviour to make it testable without saying so explicitly and justifying it.", "Keep test dependencies minimal; prefer the standard test harness over adding crates."]
  summary: "Added a server-free, table-driven test suite (40 unit + 1 integration = 41 tests) covering the task's identity, placement, ordering/rank, decoration and paused acceptance criteria, using only Rust's standard harness and no new dependencies. New `src/testutil.rs` (cfg(test)-only builders/temp dir) is wired via `#[cfg(test)] mod testutil;`; test modules were added to identity.rs, forest.rs, decoration.rs, pause.rs, projection.rs, transport.rs and lifecycle.rs, plus `tests/cli_flags.rs` for the apply/clear paused-flag boundary. `README.md` now documents `cargo test --locked` and states no Herdr server is needed. The only runtime-code change is the approved behaviour-preserving extraction: the ceiling condition `placements.len() > MAX_RANKS` became `!projection::within_rank_ceiling(placements.len())`, where `within_rank_ceiling(count) = count <= MAX_RANKS` — identical condition, log line, digest update and early return. No server harness, no behaviour change, demo.sh/install.sh/herdr-plugin.toml/Cargo.* untouched. Remaining gaps are explicit and not faked: the decoration attention-drop stage is unreachable with current constants; the >999999-rank early return is covered via its predicate, not a million-row fixture; and the \"no path/routing id/delivery id/report content\" guarantee is structural, tested through adversarial task_id truncation since decoration has no injection point for those fields."
  evidence: ["Hash recomputation is byte-exact compact JSON tuple, lowercase hex, no trimming/canonicalization, no truncation: identity::tests::self_hash_is_byte_exact_lowercase_hex_over_the_raw_tuple asserts the hardcoded independent digest 82985f7d...8970e3 for [\"pi\",\"path\",\"/tmp/session.jsonl\"], 64 lowercase hex chars; self_hash_does_not_trim_case_fold_or_canonicalize_the_path asserts /a/../b != /b, /a//b != /a/b, /a/b/ != /a/b, case difference, and 64-char length for edge paths; lower_hex64_accepts_only_exactly_64_lowercase_hex rejects 63/65/uppercase/non-hex/empty.", "agency_self self-validation before trusting relationship metadata: relationship_requires_all_three_tokens asserts each missing token is unlinked; self_validation_accepts_only_a_recomputed_lowercase_hex_identity accepts worker/subagent with matching hash and rejects role reviewer/empty, uppercase/short/non-hex agency_self or agency_parent, recomputed mismatch and no session path.", "Unique parent resolution and tokenless-parent refinement: tokenless_parent_is_validated_through_a_validated_child shows a root with zero tokens emitted at depth 0 with role \"\" and no attention, its child at depth 1; ambiguous_parent_matches_are_unlinked and dangling/recomputed-mismatch cases in malformed_and_missing_identities_are_unlinked show non-unique or non-matching edges leave the child unlinked.", "Every C5 degenerate case resolves to unlinked: Covered by relationship_requires_all_three_tokens (missing), malformed_and_missing_identities_are_unlinked (malformed role/hex, recomputed mismatch, dangling), duplicate_agency_self_unlinks_every_carrier (duplicate, with a passing unique-child control), self_link_produces_no_parent_edge (self-link), ambiguous_parent_matches_are_unlinked (ambiguous), cycles_never_rank_and_never_validate_a_parent (cycle), stale_identity_is_recomputed_from_each_snapshot (stale/replaced, rebuild yields child unlinked).", "Preorder emission, family contiguity, deterministic sibling order, unranked native ordering: emission_is_preorder_family_contiguous_and_native_ordered asserts exact order [root-a,worker-2,worker-1,sub-a,root-b,sub-b], depths [0,1,1,2,0,1], ranks 1..6 and is_last_sibling flags, with the lone row excluded; sibling_order_follows_native_order_and_is_deterministic asserts native order is authoritative and repeatable; projection::view_sort_spec_is_exact_and_puts_rank_before_native_order pins the rank-first sort and the workspace/tab/pane tie-breakers; unlinked_rows_receive_no_rank... asserts unlinked rows are absent from the projection.", "Rank format: 6-digit zero padding, lexicographic == numeric, no publication above the ceiling: ranks_are_six_digit_and_lexicographic_order_matches_numeric_order checks ranks 1..=1000 format identically to {rank:06}, are 6 chars, and sort in numeric order; rank_ceiling_is_exact_at_the_documented_maximum asserts within_rank_ceiling accepts MAX_RANKS=999999 and rejects 1000000.", "Decoration grammar: 20-char cap, drop order task then attention then indent collapse, branch/role never dropped, no leading whitespace, no hash/path/routing id/delivery id/report content: depth_indent_branch_and_role_grammar pins the exact glyph strings incl. the depth>3 ellipsis collapse; worker_task_is_truncated_to_twelve... and attention_is_appended_when_it_fits cover task/attention grammar; over_cap_drops_the_task_first_and_keeps_branch_and_role shows the task is dropped first while branch/role remain; every_rendered_value_respects_the_cap_and_never_leads_with_whitespace iterates depths/roles/tasks/attention; decoration_never_emits_a_full_hash_or_untruncated_task and decoration_never_emits_a_path_routing_id_delivery_id_or_report_content assert no full adversarial metadata value survives the 20-char cap.", "Paused behaviour: paused publishes nothing and sets no view; apply clears the flag; clear does not: lifecycle::paused_pass_publishes_nothing_and_sets_no_view calls pass with an existing paused flag and an unreachable socket and asserts Ok with empty model/digest and no owned/passive view; pause::set_creates_and_removes_the_flag_and_flip_toggles covers flag create/remove/idempotence and toggle; tests/cli_flags.rs apply_clears_the_paused_flag_but_clear_does_not runs the real binary and asserts apply exits 1 after clearing the flag while clear exits 1 and leaves it.", "Single documented command, no running Herdr server, minimal dependencies: README.md documents `cargo test --locked` and states it needs no server/socket/network; Cargo.toml has no dev-dependencies and Cargo.lock is unchanged; the integration test uses an absent socket and a live holder lock, and no server process is started.", "No plugin behaviour changed to make it testable: Only runtime change is the extraction to projection::within_rank_ceiling with the identical condition (`!within_rank_ceiling(len)` == `len > MAX_RANKS`); same log message, same `*last_digest = digest`, same early return. All other production lines are untouched; `git diff --name-only` shows demo.sh/install.sh/herdr-plugin.toml/Cargo.* unmodified."]
  filesChanged: ["src/testutil.rs", "src/main.rs", "src/identity.rs", "src/forest.rs", "src/decoration.rs", "src/pause.rs", "src/projection.rs", "src/transport.rs", "src/lifecycle.rs", "tests/cli_flags.rs", "README.md"]
  updatedAt: "2026-09-16T02:14:53Z"
createdAt: "2026-09-16T01:23:25Z"
updatedAt: "2026-09-16T02:14:53Z"
assignee: "worker-task-1-0ec8d999"
archivedAt: "2026-09-16T02:14:53Z"
resolution:
  outcome: "completed"
---

## Description

## Problem

This plugin has no tests. The focused harness written during the original implementation was explicitly temporary and deleted before delivery, under a constraint against permanent harness expansion for the MVP. That was right for an MVP and is wrong for a release.

Everything currently rests on observation in an isolated Herdr instance. That caught real defects, but it is not repeatable in CI and does not protect the identity rules against future edits.

## What matters most

The identity and placement rules are where a silent regression would be worst, because a wrong parent edge produces a confident, wrong tree rather than an obvious failure. Cover at least:

- Hash recomputation: byte-exact compact JSON tuple, lowercase hex, no path trimming or canonicalization, no hash truncation.
- Self-validation: `agency_self` must match the recomputed identity before any relationship metadata is trusted.
- Parent-edge validation, including the refinement where a parent carrying no tokens is validated through a validated child's `agency_parent`.
- Every degenerate case from the contract C5 table, each resolving to unlinked: missing, malformed, mismatched, duplicate, self-link, dangling, ambiguous, cycle, stale. Duplicate detection was already caught as a real defect by the temporary harness, so the risk is demonstrated rather than hypothetical.
- Preorder emission: parent before descendants, family contiguity, deterministic sibling order, unranked rows keeping native relative order.
- Rank format: 6-digit zero padding, lexicographic order equal to numeric order, no publication above the documented ceiling.
- Decoration grammar: 20-character cap, drop order task then attention then indent collapse, branch and role never dropped, never emits leading whitespace, never emits a hash, path, routing id, delivery id or report content.
- Paused behaviour: paused publishes nothing and sets no view; `apply` clears the flag; `clear` does not.

## Scope boundary

Unit and integration tests against the plugin's own logic. Do not build a Herdr server harness and do not require a live or isolated server to run the suite: `demo.sh` already covers end-to-end observation and stays the place for that. If a rule genuinely cannot be tested without a server, say so rather than faking it.

Reference: `docs/agent-tree/m2-contract.md` holds the authoritative rules.

