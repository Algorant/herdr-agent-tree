---
id: task-6
type: task
title: "Add a safe latest-checkout dogfood reload workflow"
priority: "high"
effort: "medium"
relatedFiles: ["justfile", "scripts/deploy.sh", "src/lifecycle.rs", "src/main.rs", "herdr-plugin.toml", "README.md", "tests/shell"]
tags: ["development", "dogfood", "lifecycle"]
accord:
  status: "accepted"
  acceptance: ["`just deploy` rebuilds and stages the latest code, registers that stage, and leaves exactly one subscriber running from the new staged build.", "Repeating `just deploy` replaces an older live subscriber safely; a dead/stale lock is recovered, while an unverifiable or foreign lock holder fails clearly without signaling it.", "The workflow clears the paused state when applying the new build and confirms that the projection pass succeeded.", "A user-owned sidebar configuration is preserved byte-for-byte; when it lacks `$agent_tree_row`, the workflow prints the exact fragment or edit required for the tree to render.", "Automated isolated coverage exercises first install, repeated reload, stale-lock handling, foreign-holder refusal and cleanup without a running live Herdr server.", "README.md documents `just deploy` as the sole rapid checkout dogfood loop and distinguishes it from release installation and inert local staging."]
  claimedAt: "2026-09-16T17:46:11Z"
  deliveredAt: "2026-09-16T18:03:48Z"
  validation: ["$ git diff --check", "$ just test", "$ ./tests/shell/dev-reload.sh"]
  constraints: ["Never kill a PID based only on an unverified integer from a lock file; verify plugin ownership, socket scope and process identity before signaling it.", "Do not restart or stop the whole Herdr server as the normal iteration path.", "Do not overwrite a user-owned [ui.sidebar.agents] block or silently substitute a row layout.", "Do not weaken the single-subscriber lock or create a second supervisor/reconnect path.", "Tests must use isolated state/processes and must not touch the live Herdr socket, config or installed plugin."]
  summary: "Delivered a verified latest-checkout reload so `just deploy` guarantees the sole subscriber runs the newly staged build. `lifecycle::reload` (new `agent-tree.reload` action, invoked by scripts/deploy.sh) clears pause, verifies any live lock holder, replaces it, and confirms one projection pass. Verification requires all of: same UID, `HERDR_PLUGIN_ID=agent-tree`, exact `HERDR_SOCKET_PATH` and `HERDR_PLUGIN_STATE_DIR`, `agent-tree subscriber` argv, and a normalized `/proc/<pid>/exe` under this reload's own `stage/` root or its exact `.stage-old.*` sibling; a holder's self-reported env cannot widen that executable set. A dead/stale lock is recovered; an unverifiable or foreign holder fails without being signaled and without spawning. SIGTERM exit waits are bounded, and reload returns only after the replacement owns the lock. deploy.sh prints the exact rows fragment when a user-owned `[ui.sidebar.agents]` block lacks `$agent_tree_row` while preserving it byte-for-byte, and requires the reload action in staged-manifest validation. apply/toggle semantics and the release installer's disabled-by-default registration are unchanged. tests/shell/dev-reload.sh drives the real deploy.sh against a fake herdr, fake cargo and a local socket server. No live Herdr socket/config/plugin was read or written."
  evidence: ["`just deploy` rebuilds and stages the latest code, registers that stage, and leaves exactly one subscriber running from the new staged build.: tests/shell/dev-reload.sh scenario 1 runs the real scripts/deploy.sh with a fake herdr/fake cargo; asserts one lock, one live pid, `readlink /proc/<pid>/exe` == $STAGE/src/agent-tree, staged_subscribers()==1, and deploy prints 'subscriber replaced'. reload returns only after wait_for_new_subscriber confirms the new pid owns the lock.", "Repeating `just deploy` replaces an older live subscriber safely; a dead/stale lock is recovered, while an unverifiable or foreign lock holder fails clearly without signaling it.: Scenario 2 replaces PID1 with PID2, PID1 exits and only one staged subscriber remains. Scenario 3 SIGKILLs PID2, leaves the lock, and the next deploy recovers it with a new live pid. Scenarios 7/8 write locks for same-user `sleep 300` holders (one with spoofed HERDR_PLUGIN_* env, one without): deploy exits nonzero, the holder is still alive, the lock remains, and stderr says 'refusing' and names the pid.", "The workflow clears the paused state when applying the new build and confirms that the projection pass succeeded.: lifecycle::reload clears pause via pause::set(path,false) then calls pass(), which returns Err if agent.list fails; the hermetic fake server answers agent.list and the pass succeeds, and the e2e apply/toggle behavior is unchanged (scenario suite and e2e pass).", "A user-owned sidebar configuration is preserved byte-for-byte; when it lacks `$agent_tree_row`, the workflow prints the exact fragment or edit required for the tree to render.: Scenario 5 hashes an unmanaged [ui.sidebar.agents] block before/after deploy (equal), confirms no `$agent_tree_row` was written, and asserts stdout contains `rows = [[\"state_icon\", \"$agent_tree_row\", \"terminal_title_stripped\"]]`. Scenario 6 confirms a block already containing `$agent_tree_row` is left alone and reported as already complete.", "Automated isolated coverage exercises first install, repeated reload, stale-lock handling, foreign-holder refusal and cleanup without a running live Herdr server.: tests/shell/dev-reload.sh (8/8) runs against a fake `herdr`, fake `cargo` and a Python Unix-socket server in a temp sandbox with HERDR_BIN_PATH cleared; it is wired into scripts/check.sh and ran inside `just test`. No live Herdr socket/config/plugin is used.", "README.md documents `just deploy` as the sole rapid checkout dogfood loop and distinguishes it from release installation and inert local staging.: README 'Development install from a checkout' now describes the verified subscriber replacement, the required identity signals, the dead/foreign handling, distinguishes `scripts/release/install.sh` (checksum-pinned, disabled-by-default, never edits config) from `scripts/stage-local.sh` (inert, never links or touches state), and notes the source-checkout subscriber caveat. CHANGELOG records the change.", "Correction: holder environment must not widen the trusted executable set; state directory must be present and path-equal.: trusted_binary no longer reads HERDR_PLUGIN_ROOT from the holder; it derives trust only from std::env::current_exe()'s stage root and its .stage-old.* sibling (unit test trusted_binary_accepts_only_the_current_stage_and_its_replaced_sibling covers accept/reject paths). verify_holder now requires HERDR_PLUGIN_STATE_DIR (None => refuse 'is unset'), with same_path equality; scenario 7 proves a spoofed-env same-user holder is still refused.", "Release installation stays disabled by default and apply/toggle semantics are unchanged.: scripts/release/install.sh is untouched and still links `--disabled`; install.sh/test-release.sh pass. lifecycle::apply and lifecycle::toggle were not modified; the e2e test invokes agent-tree.apply and still passes."]
  filesChanged: ["src/lifecycle.rs", "src/main.rs", "herdr-plugin.toml", "scripts/deploy.sh", "scripts/check.sh", "scripts/stage-local.sh", "tests/shell/dev-reload.sh", "README.md", "CHANGELOG.md", "justfile"]
  updatedAt: "2026-09-16T18:03:48Z"
createdAt: "2026-09-16T12:39:00Z"
updatedAt: "2026-09-16T18:03:48Z"
blockers: ["task-9"]
assignee: "worker-task-6-06e2f08c"
archivedAt: "2026-09-16T18:03:48Z"
resolution:
  outcome: "completed"
---
After task-9 establishes `just deploy` and `scripts/deploy.sh`, strengthen that workflow so it reliably replaces the long-lived subscriber. Restaging alone does not replace an already-running subscriber: the old in-memory binary can continue holding subscriber-<socket>.lock, the new apply action performs only one pass, and later events return to old code. `just deploy` must build, stage, register, and guarantee that the sole subscriber is the newly staged build without restarting the whole Herdr server or disturbing panes. A user-owned [ui.sidebar.agents] block must remain untouched; the workflow reports the exact row fragment needed when `$agent_tree_row` is absent.