---
id: task-11
type: task
title: "Implement the grouped, priority and tree agent-mode cycle"
priority: "high"
effort: "medium"
relatedFiles: ["docs/agent-tree/task-10-mode-cycle.md", "herdr-plugin.toml", "src/lifecycle.rs", "src/projection.rs", "src/wire.rs", "README.md", "tests"]
tags: ["ux", "configuration", "toggle"]
accord:
  status: "accepted"
  acceptance: ["Starting from grouped, successive action invocations produce priority, then tree, then grouped again with the correct sidebar header each time.", "Grouped and priority ordering match Herdr's native modes; tree ordering and identity validation remain unchanged.", "Leaving tree mode clears plugin-owned tree tokens and projection state so grouped/priority do not retain stale decoration or rank data.", "The cycle remains deterministic across repeated invocations, config reload and server restart, with documented initial-state behavior.", "The user's sidebar row configuration is preserved while modes change.", "Automated isolated tests cover the complete cycle, view ownership conflicts, restart/reload, stale tree-token cleanup and unknown/corrupt mode state."]
  claimedAt: "2026-09-17T22:02:17Z"
  deliveredAt: "2026-09-17T22:22:33Z"
  validation: ["$ git diff --check", "$ just test", "$ just build"]
  constraints: ["Follow the task-10 mechanism and state contract; do not invent a fallback implementation.", "Do not approximate or rename the grouped and priority native modes.", "Do not overwrite the user's [ui.sidebar.agents] row configuration as part of cycling modes.", "Do not restart or stop the whole Herdr server as the mode-change path.", "Keep mode state, active view ownership and displayed header consistent across repeated actions and server restarts."]
  summary: "Implemented the task-10 §2 mode cycle. A new `agent-tree.cycle` action (replacing `toggle`, no alias) advances exactly grouped -> priority -> tree -> grouped: the socket-scoped pause flag is the tree/native discriminator, `ui.agent_panel_sort` tells grouped from priority, grouped/priority write only that key and call `server.reload_config`, and tree clears the pause flag and runs the existing projection. A read-only foreign-source `agent.view.clear` probe refuses a foreign view owner and clears a paused+own-view desync. `src/config.rs` is a new byte-preserving line editor for the single `agent_panel_sort` line inside `[ui]` plus a socket-scoped `original-sort-<tag>` record; `clear` restores the original value (removing the key if absent), reloads config and removes the record, and `scripts/deploy.sh --uninstall` keeps invoking clear without a second config writer. Herdr's native `grouped`/`priority` headers and row order were re-observed on the installed Herdr (0.9.1) and match the 0.9.0 finding. Hermetic Rust tests cover sort classification (absent/workspaces/unknown -> grouped), byte-exact edit/insert/append/restore, corrupt-record refusal, the mode-decision table and view-owner classification. A new isolated `tests/e2e/mode-cycle.sh`, wired into scripts/check.sh, drives the full cycle twice with tmux header/order assertions, config preservation and byte-identical clear, stale-token cleanup, unknown sort treated as grouped, corrupt-record fail-closed, foreign-owner refusal, config reload without restart, and pause-state survival across a server restart. `git diff --check`, `just test` (formatting, Clippy, tests, locked build, shell/release/installer suites, and the deploy-reload, sidebar and mode-cycle e2e tests) and `just build` all pass. Remaining risk: the sort key is user-global while the pause flag/record are socket-scoped (task-10 risk 3)."
  evidence: ["Starting from grouped, successive action invocations produce priority, then tree, then grouped with the correct sidebar header: tests/e2e/mode-cycle.sh pass 1 and pass 2 both confirmed via tmux PTY: grouped -> priority (header priority) -> tree (header tree) -> grouped (header grouped); cycle log statuses were succeeded.", "Grouped and priority ordering match Herdr's native modes; tree ordering and identity validation unchanged: The isolated test observed native headers grouped/priority and order idle-one,blocked-two,working-three,unknown-four (workspace order) for grouped and blocked-two,working-three,idle-one,unknown-four (attention order) for priority on the installed Herdr 0.9.1, matching the task-10 0.9.0 finding. tests/e2e/sidebar.sh still passes 5 ranks and the tree view label.", "Leaving tree mode clears plugin-owned tree tokens and projection state: After seeding agent_tree_row=stale/agent_tree_rank=000001 with no subscriber, a tree -> grouped cycle left the pane tokens as -|-; the cycle also source-clears the view.", "Deterministic across repeated invocations, config reload and server restart, with documented initial-state behavior: Two full cycles produced identical headers/orders; the server pid was unchanged across all cycles (config reload only); after stop/start the pause flag and spaces value survived, the startup hook started no subscriber, the header stayed grouped, and cycling resumed. README documents the no-pause-flag = tree initial state and first invocation -> grouped.", "The user's sidebar row configuration is preserved while modes change: Every cycle asserted the exact rows block still present; after clear, cmp of config.toml against the pre-plugin copy passed byte-for-byte.", "Automated isolated tests cover the complete cycle, view ownership conflicts, restart/reload, stale tree-token cleanup and unknown/corrupt mode state: tests/e2e/mode-cycle.sh (wired into scripts/check.sh) covers all of these: full cycle x2, grouped/priority headers and order, foreign view-owner refusal with the view and config unchanged, reload without restart, server restart persistence, stale-token cleanup, unknown sort treated as grouped (no error), and a corrupt original-sort record failing closed. Hermetic Rust tests cover classify/decision logic, absent values, byte-exact config edit/restore and corrupt-record refusal.", "Do not invent a fallback, do not approximate native grouped/priority, do not rewrite [ui.sidebar.agents]: Cycle delegates grouped/priority to ui.agent_panel_sort + server.reload_config and only ever edits that one line; src/config.rs has no MODE_FILE and no toggle alias. scripts/deploy.sh, scripts/stage-local.sh and scripts/release/install.sh changed only toggle -> cycle in action lists/strings; scripts/deploy.sh's invoke_reload/verify_subscriber path is untouched."]
  filesChanged: ["CHANGELOG.md", "README.md", "docs/release.md", "herdr-plugin.toml", "scripts/check.sh", "scripts/deploy.sh", "scripts/release/install.sh", "scripts/stage-local.sh", "src/config.rs", "src/lifecycle.rs", "src/main.rs", "src/pause.rs", "src/projection.rs", "tests/e2e/mode-cycle.sh"]
  updatedAt: "2026-09-17T22:22:33Z"
createdAt: "2026-09-16T17:58:11Z"
updatedAt: "2026-09-17T22:22:33Z"
references: ["task-10", "decision-1"]
assignee: "worker-task-11-18e21e3e"
archivedAt: "2026-09-17T22:22:33Z"
resolution:
  outcome: "completed"
---
Implement the mechanism selected by task-10 so the plugin action cycles exactly `grouped → priority → tree → grouped`. Grouped and priority use Herdr-native-equivalent ordering and labels; tree uses the validated delegation projection. The sidebar row configuration remains independent and must not be overwritten as part of changing modes. Replace the current binary tree/native toggle before public release.