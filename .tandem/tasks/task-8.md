---
id: task-8
type: task
title: "Publish v0.1.0 as a public Herdr plugin"
state: "todo"
priority: "medium"
effort: "medium"
blockers: ["task-8-1", "task-8-2"]
relatedFiles: ["herdr-plugin.toml", "Cargo.toml", "CHANGELOG.md", "README.md", "docs/release.md", ".github/workflows/ci.yml", ".github/workflows/release.yml", ".tandem", ".gitignore"]
tags: ["release", "public", "marketplace", "source-build"]
accord:
  status: "ready"
  acceptance: ["Task-8-1 and task-8-2 are complete, Algorant explicitly approves the exact managed-install dogfood result and public release, and all required hosted CI jobs pass on the reviewed release commit.", "Internal `.tandem` coordination metadata is removed from the public release tree and excluded from future public commits only at the final boundary, without losing the accepted dogfood/release decision record needed before that boundary.", "Algorant/herdr-agent-tree is public with the `herdr-plugin` topic and a parseable root `herdr-plugin.toml` whose standard source-build install contract matches decision-3.", "The immutable `v0.1.0` tag points at the reviewed release commit; the GitHub release workflow completes successfully and publishes the source release without custom binary installer assets.", "A clean anonymous `herdr plugin install Algorant/herdr-agent-tree --ref v0.1.0` runs the manifest build, registers a runnable plugin, and the documented activation/doctor flow succeeds without any pre-existing checkout or target directory.", "README.md, CHANGELOG.md and docs/release.md describe the published source-build release, standard Herdr install/update commands and actual support status, with no stale private/no-release/planned-install claims."]
  claimedAt: "2026-09-21T02:17:56Z"
  deliveredAt: "2026-09-21T02:35:37Z"
  validation: ["$ git diff --check", "$ just test", "$ just build", "$ scripts/release/check-release.sh --ref refs/tags/v0.1.0"]
  constraints: ["Do not perform the final visibility change, tag or release until Algorant explicitly approves the exact task-8-2 managed-install result and authorizes publication.", "Use the single Herdr-managed source-build path from decision-3; do not restore custom installers, binary archives, target promotion allowlists or plugin-link release paths.", "Keep `.tandem` tracked until every prerequisite coordination record is accepted; remove it only in the final reviewed release commit.", "Do not replace, move or recreate an existing tag or GitHub release; fail rather than overwrite.", "Keep the repository private until the owner-authorized final publication sequence."]
  summary: "task-8-1 source-install convergence is implemented and committed on this branch. herdr-plugin.toml now declares exactly one [[build]] command `[\"cargo\", \"build\", \"--locked\", \"--release\"]` while keeping the tracked `./src/agent-tree` launcher as the manifest entrypoint (it execs the built target/release binary), so `herdr plugin install Algorant/herdr-agent-tree --ref v0.1.0` is the sole normal-user path. Cargo.toml gained description/license/repository/readme while keeping rust-version 1.81 and publish=false. The bespoke checksum installer, musl archive packaging, target-promotion/native-evidence gate, their exclusive tests, the musl candidate CI job, release/targets.txt and .cargo/config.toml are removed; check-release.sh was rewritten for the source-build contract; a hermetic clean-source install smoke test parses and executes the manifest [[build]] argv from a target-less tracked-files export and proves ./src/agent-tree reaches the produced binary; stage-local coverage was extracted into its own test. README/CHANGELOG/docs/release.md now describe the Herdr-managed source path with truthful pre-publication wording (no published claim). Development deploy/endpoint/doctor tooling and all plugin runtime/identity behavior are unchanged. No tag, release or visibility change, and .tandem is untouched. Remaining gap: the exact Herdr-managed anonymous install is task-8-2 dogfood, not performed here; and the CHANGELOG v0.1.0 heading stays `- planned` until the parent's final publication phase."
  evidence: ["task-8-1 AC1: manifest declares an idiomatic [[build]] with cargo build --locked --release and a clean checkout yields the executable every manifest command runs: herdr-plugin.toml now has one `[[build]] command = [\"cargo\", \"build\", \"--locked\", \"--release\"]`. tests/shell/source-install.sh exports only git-tracked files to a target-less tree, runs the parsed manifest build argv verbatim, asserts target/release/agent-tree exists, asserts every runtime manifest command is a plugin-relative executable, and proves ./src/agent-tree reaches that build (unknown subcommand is handled as a usage error). `just test` shows `ok - clean source build produced the executable every manifest command runs`.", "task-8-1 AC2: README documents herdr plugin install Algorant/herdr-agent-tree --ref v0.1.0 as the sole normal-user path with update and activation: README.md 'Install' now has only 'Herdr-managed source install (the normal user path)' with that exact command, a manual activation flow (`herdr plugin enable agent-tree`, `herdr server reload-config`, `herdr plugin action invoke agent-tree.apply`), reinstall-to-update, and `herdr plugin uninstall agent-tree`; the checksum installer/archive sections were deleted.", "task-8-1 AC3: bespoke installer/archive/target-evidence path removed rather than retained; dev deploy and endpoint doctor remain non-user paths: Committed deletions: scripts/release/{install,package,check-targets,checksums,check-binaries}.sh, release/targets.txt, docs/release-evidence/README.md, tests/shell/{test-install,test-release,install-candidate}.sh, .cargo/config.toml. scripts/deploy.sh, deploy-endpoint.sh, doctor.sh and scripts/lib/* are functionally unchanged (deploy.sh only had two stale installer references updated); stage-local coverage was preserved in tests/shell/stage-local.sh and the hermetic endpoint/doctor suites still pass inside `just test`.", "task-8-1 AC4: Cargo.toml standard package metadata while preserving rust-version and non-crates.io status: Cargo.toml adds description, license = \"MIT\" (LICENSE present), repository = \"https://github.com/Algorant/herdr-agent-tree\" and readme = \"README.md\"; rust-version = \"1.81\" and publish = false are unchanged. A `cargo +1.81.0 build --locked --release` completed with no warnings.", "task-8-1 AC5: CI and the tag workflow validate the source release contract without custom binary assets and a clean-install smoke check: ci.yml now has only the pinned-1.81 quality job (build + scripts/check.sh --no-e2e), which runs the new source-install smoke; the owner-gate step and musl candidates matrix are gone. release.yml's validate job runs scripts/release/check-release.sh --ref \"$GITHUB_REF\" plus tests/shell/source-install.sh, and its publish job creates the tag release with no uploaded assets and refuses an existing release.", "Constraints: no publication/visibility change/tag, .tandem retained, no runtime/identity behavior change: No tag or GitHub release was created and repository visibility is untouched. `.tandem` was excluded from `git add` and the commit; `git status` is clean and no `.tandem` path is in the commit. herdr-plugin.toml runtime commands and src/lifecycle.rs are unchanged, so the existing parent-of-parent `.stage-old.*` trust boundary is preserved; all isolated Herdr deploy/reload, sidebar and toggle end-to-end tests pass under `just test`.", "Pre-publication docs remain truthful: README and docs/release.md explicitly state no tagged release has been published and the repository stays private until Algorant's approval, and docs/release.md records the owner-gated publication sequence without claiming it happened."]
  filesChanged: ["herdr-plugin.toml", "Cargo.toml", "CHANGELOG.md", "README.md", "docs/release.md", "scripts/release/check-release.sh", "scripts/check.sh", "scripts/deploy.sh", "tests/shell/source-install.sh", "tests/shell/stage-local.sh", ".github/workflows/ci.yml", ".github/workflows/release.yml", "scripts/release/install.sh", "scripts/release/package.sh", "scripts/release/check-targets.sh", "scripts/release/checksums.sh", "scripts/release/check-binaries.sh", "release/targets.txt", "docs/release-evidence/README.md", "tests/shell/test-install.sh", "tests/shell/test-release.sh", "tests/shell/install-candidate.sh", ".cargo/config.toml"]
  note: "Integrated and accepted the task-8-1 source-install convergence. Parent publication remains open and must not complete until task-8-2 dogfoods the exact Herdr-managed install and Algorant explicitly approves publication."
  updatedAt: "2026-09-21T02:44:25Z"
createdAt: "2026-09-16T12:39:34Z"
updatedAt: "2026-09-24T16:02:23Z"
references: ["decision-3"]
---
Publish v0.1.0 through Herdr's native plugin lifecycle after the source-build install contract and its exact managed-install dogfood are complete and Algorant explicitly approves release. The sole normal-user path is `herdr plugin install Algorant/herdr-agent-tree --ref v0.1.0`; the tag workflow publishes the immutable reviewed source release rather than custom binary archives. At the final public boundary, remove internal `.tandem` coordination metadata from the release tree and ignore it in future commits, make the repository public, create the exact tag, verify hosted CI/release completion, confirm marketplace discovery prerequisites, and perform a clean anonymous Herdr-managed installation.

Algorant reviewed the exact cb630ad managed-install dogfood (including real Pi delegation tree, toggle, safe managed reinstall/reload, cleanup, healthy doctor and hosted CI) and explicitly approved public v0.1.0 publication on 2026-09-24. Algorant accepts that historical `.tandem` commits remain visible after `.tandem/` is removed from the release tip and added to `.gitignore`.