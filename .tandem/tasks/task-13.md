---
id: task-13
type: task
title: "Replace the three-mode cycle with an owner-safe Agent Tree toggle"
state: "in-progress"
priority: "high"
effort: "medium"
references: ["task-11", "task-10", "decision-2"]
relatedFiles: ["src/main.rs", "src/lifecycle.rs", "src/pause.rs", "src/projection.rs", "src/config.rs", "herdr-plugin.toml", "tests/e2e/mode-cycle.sh", "scripts/check.sh", "README.md", "CHANGELOG.md"]
tags: ["ux", "configuration", "toggle"]
accord:
  status: "rework"
  acceptance: ["A registered `agent-tree.toggle` action and documented `[[keys.command]]` binding (`prefix+alt+t`) switch no active plugin view → Agent Tree view → native Agents list without restarting Herdr.", "Turning Agent Tree off preserves the Herdr client's existing native grouped/priority choice and neither writes `ui.agent_panel_sort` nor creates or consumes sort-restore state.", "The subscriber continues publishing `agent_tree_row` and `agent_tree_rank` with Agent Tree ordering both on and off; only the plugin-owned view changes.", "If another source owns the Agents view, toggle fails clearly and leaves that view unchanged; an unknown or ambiguous owner also fails closed.", "The obsolete `agent-tree.cycle` action, three-mode state machine, configuration writer/restore path, and corresponding documentation are removed rather than retained as a compatibility path.", "Isolated real-Herdr coverage drives the configured shortcut through native → tree → native, verifies tree ordering and retained decorations, verifies the native mouse grouped/priority toggle works again when tree is off, and verifies a foreign view is never displaced."]
  claimedAt: "2026-09-18T15:19:48Z"
  deliveredAt: "2026-09-18T15:27:37Z"
  validation: ["$ git diff --check", "$ just test", "$ just build"]
  constraints: ["Do not overwrite, clear, or approximate a view owned by another source.", "Do not write `ui.agent_panel_sort` or attempt to synchronize Herdr's client-local header preference.", "Do not retain `agent-tree.cycle` as an alias, fallback, or compatibility path.", "Do not restart the whole Herdr server as the toggle path.", "Do not overwrite `[ui.sidebar.agents]` rows.", "Keep view operations source-checked and preserve the existing one-subscriber and full-clear guarantees."]
  summary: "Applied /tmp/task-13-owner-safe-toggle.patch as-is (SHA-256 ab6d0f17...c729867) and committed it as b654038. Exactly the expected 19 non-.tandem paths changed; no adaptation was needed and none was made. Replaced the three-mode `agent-tree.cycle` and its ui.agent_panel_sort write/capture/restore machinery with one owner-safe `agent-tree.toggle` action documented as the `prefix+alt+t` key command; the manifest exposes start/apply/reload/clear/toggle with no cycle alias. The subscriber now publishes agent_tree_row/agent_tree_rank in both states, tree-off is a socket-scoped marker (src/mode.rs) beside the subscriber lock, foreign/unknown view owners fail closed, apply still means show tree, and clear stays the full token+view cleanup. Deleted src/config.rs, src/pause.rs, docs/agent-tree/task-10-mode-cycle.md and tests/e2e/mode-cycle.sh; added src/mode.rs and tests/e2e/toggle.sh. All three native validations ran in order and passed: `git diff --check` (exit 0, no output), `just test` (exit 0, including the new isolated Herdr toggle E2E), `just build` (exit 0). Working tree is clean at b654038. The isolated tests never contacted the active Herdr server: its socket inode/mtime, server PID, config.toml hash and plugin state dir were byte-identical before and after, and no isolated temp dirs or servers leaked."
  evidence: ["A registered agent-tree.toggle action and documented prefix+alt+t binding switch no active plugin view -> Agent Tree view -> native Agents list without restarting Herdr: herdr-plugin.toml registers id = \"toggle\" (command ./src/agent-tree toggle); README documents the [[keys.command]] entry key = \"prefix+alt+t\" running `herdr plugin action invoke agent-tree.toggle`. tests/e2e/toggle.sh installed that binding in an isolated config and pressed it through a real tmux PTY, observing header tree -> grouped -> tree -> grouped with node restart.", "Turning Agent Tree off preserves the Herdr client's existing native grouped/priority choice and neither writes ui.agent_panel_sort nor creates/consumes sort-restore state: toggle.sh asserts the header remains Herdr's own 'grouped' after toggle-off, that config.toml gained no sort key, and that config.toml is byte-identical to the pristine copy apart from Herdr's own onboarding line. src/config.rs (the only sort writer/restore code) is deleted and no call site remains.", "The subscriber continues publishing agent_tree_row and agent_tree_rank with Agent Tree ordering both on and off: lifecycle::pass no longer early-returns when off; it always reconciles tokens and only chooses ensure_view vs ensure_view_cleared. toggle.sh's assert_decorations_retained confirmed root-alpha rank 000001, sub-alpha rank 000002 and sub-alpha agent_tree_row in tree, native and tree-again states.", "If another source owns the Agents view, toggle fails clearly and leaves that view unchanged; an unknown or ambiguous owner also fails closed: toggle_enables returns Err naming the foreign source, and classify_view errors on an unknown owner, both before the marker or view is touched. toggle.sh installed a throwaway foreign plugin and observed agent-tree.toggle status=failed with stderr naming plugin:view-owner while the view probe stayed 'true plugin:view-owner'; a subsequent shortcut press left it unchanged.", "The obsolete agent-tree.cycle action, three-mode state machine, configuration writer/restore path, and corresponding documentation are removed: The commit deletes src/config.rs, src/pause.rs, docs/agent-tree/task-10-mode-cycle.md and tests/e2e/mode-cycle.sh; scripts/check.sh, scripts/deploy.sh, scripts/stage-local.sh and scripts/release/install.sh validate 'toggle' instead of 'cycle'; the manifest and CLI have no cycle route and a tree-wide grep found no cycle/pause/config compatibility path.", "Isolated real-Herdr coverage drives the configured shortcut through native -> tree -> native, verifies tree ordering and retained decorations, verifies the native mouse grouped/priority toggle works again when tree is off, and verifies a foreign view is never displaced: The just test log from tests/e2e/toggle.sh shows: tree view order root-alpha,sub-alpha,lone-1; tree->native retained decorations; native->tree retained decorations; tree->native again retained decorations; native header press toggled grouped -> priority; foreign view refused and unchanged by both action and shortcut; clear removed tokens and view; 'Toggle end-to-end test passed' then 'All checks passed'.", "Active Herdr server is never contacted by the isolated tests: Pre/post snapshots were identical: active server PID 10263, socket inode 41767 mtime 1789213489, /home/ivan/.config/herdr/config.toml sha256 b86b6002..., and every file in /home/ivan/.local/state/herdr/plugins/agent-tree is byte-identical; no isolated /tmp test dirs or processes survived."]
  filesChanged: ["CHANGELOG.md", "README.md", "docs/agent-tree/task-10-mode-cycle.md", "docs/release.md", "herdr-plugin.toml", "scripts/check.sh", "scripts/deploy.sh", "scripts/release/install.sh", "scripts/stage-local.sh", "src/config.rs", "src/lifecycle.rs", "src/main.rs", "src/mode.rs", "src/pause.rs", "src/projection.rs", "src/transport.rs", "tests/cli_flags.rs", "tests/e2e/mode-cycle.sh", "tests/e2e/toggle.sh"]
  note: "Integration recovery is complete. The failed aggregate rebase was aborted, your original validated commit b654038 was restored, and only that single Task 13 commit was replayed onto current authoritative main (67bafa0), producing HEAD 3faf9618ad1efa1d5cb7a3323d6d7ecef15c3d98. No content changed: the binary diff SHA-256 remains exactly ab6d0f17a84161d21a866badf20cf16c888b92334f8ff05738fb2d170c729867, HEAD^ equals current main, git diff --check passes, and the worktree is clean. Do not modify files or redo im"
  updatedAt: "2026-09-18T15:27:37Z"
createdAt: "2026-09-17T22:44:03Z"
updatedAt: "2026-09-18T15:27:37Z"
assignee: "worker-task-13-51edea24"
---
## Description

The grouped/priority/tree cycle shipped by task-11 is the wrong product. Isolated Herdr 0.9.1 evidence established that the native Agents header is a client-local grouped↔priority toggle, any active plugin view disables its hit target, and no plugin API can observe or join that toggle.

Replace the cycle with one supported Herdr keyboard shortcut for turning Agent Tree ordering on and off. The plugin exposes `agent-tree.toggle`, documented through a `[[keys.command]]` binding using `prefix+alt+t`:

- With no active plugin view, toggle installs this plugin's `tree` view.
- When this plugin owns the active view, toggle clears only that view and reveals Herdr's existing native Agents list in whichever grouped/priority order the client already uses.
- When another source owns the active view, toggle refuses clearly and leaves it unchanged; Herdr exposes no view stack that could restore a displaced foreign view.

The subscriber keeps publishing `agent_tree_row` and `agent_tree_rank` while the plugin is enabled, so decorations remain visible with tree ordering on or off. Remove the obsolete three-state cycle and its `ui.agent_panel_sort` write/capture/restore machinery rather than retaining an alias or fallback. `apply` continues to mean show Agent Tree, while `clear` remains the full plugin cleanup path.
